sh = find_program('sh')
gawk = find_program('gawk')
preproc_awk = files('preproc.awk')

manflags = []

#if get_option('')
#  manflags += '-D...'
#endif

#generator(gawk,   s/generator/custom_target/
#  arguments: [sh, '-c', '"@0@" -f "@1@" -- @3@ < > '.format(gawk, preproc_awk, ' '.join(manflags)), '--', 'FIXME:MANFLAGS', ]
